Consumption and prediction of energy 
this academic mini-project reflect my skills in python and data analytics using this dataset https://www.kaggle.com/datasets/mrsimple07/energy-consumption-prediction/data

Windows:
python -m venv venv
venv\Scripts\activate

Linux:
python3 -m venv venv
source venv/bin/activate

pip install --upgrade pip
pip install -r requirements.txt

streamlit run app.py